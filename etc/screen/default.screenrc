source ${HOME}/etc/screen/global.screenrc

# Startup!
chdir ${HOME}/code/${PROJECT}
screen -t "shell"
screen -t "vim" /usr/bin/vim
screen -t "notes" /usr/bin/vim ${HOME}/notes
screen -t "watch"

select 0
split
resize 20%
focus down
select 1

